syntax = "proto3";

package patient.events;
// Tells maven plugin we want to generate seperate files instead of having under one parent class
option java_multiple_files = true;

message PatientEvent {
  string patientId = 1;
  string name = 2;
  string email = 3;
  string event_type = 4;
}